#include <stdio.h>
 
int main() {
    int cod, qtd;
    float valor_peca, valor, total;

    scanf("%d %d %f", &cod, &qtd, &valor_peca);
    valor += valor_peca*qtd;

    scanf("%d %d %f", &cod, &qtd, &valor_peca);
    valor += valor_peca*qtd;

    printf("VALOR A PAGAR: R$ %.2f\n", valor);

    return 0;
}